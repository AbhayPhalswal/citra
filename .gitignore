# --- personal / secret: never commit these -----------------------------
# NOTE: a comment must be on its OWN line. git treats "file.txt  # note"
# as a pattern that literally ends in "# note", which matches nothing -
# that is exactly how these two were silently unignored once.
citra_contacts.json
citra_api_token.txt
citra_cert.pem
citra_key.pem
citra_muted.json
citra_model_preference.json
call_recordings/
generated_code/
my_voice_positive/
my_voice_negative/
*.log

# --- environment -------------------------------------------------------
jarvis_venv/
__pycache__/
*.pyc
*.tmp
.claude/settings.local.json

# --- large model files (download them, see README) ---------------------
*.onnx
!hey_citra.onnx

# your WiFi credentials
*/wifi_secrets.h

# Claude Code's project folder - local tooling, not part of the project
.claude/

# --- local dev / test environment ---------------------------------------
.venv/
.pytest_cache/
.ruff_cache/
